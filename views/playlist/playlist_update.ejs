<!DOCTYPE html>
<html>
<head>
    <title>Playlist - Edit</title>
</head>
<h1>Edit Playlist</h1>
<body>

<form action="/playlist/update" method="get">

    <input type="hidden"
           id="playlist_id"
           name="playlist_id"
           value="<%= playlists.playlist_id %>"
    />

    <span style="font-weight:bold">User</span> <br />

    <select name= 'user_name'>
        <% for(var i=0; playlist_result.length > i; i++ ) {
            var selected = "";
            if(playlist_result[i].user_name != null)
            {
                selected = "selected";

            }%>
        <option  <%= selected %> value="<%= playlist_result[i].user_name%>"> <%= playlist_result[i].user_name %>

            <% } %>
    </select>
    <br /><br />

    <span style="font-weight:bold">Name: </span></br><input
            id="name"
            name="name"
            type="text"
            value="<%= playlists.name%>"
    />
    </br><br />

    <span style="font-weight:bold"> Songs </span> <br /> <br />
    <% for(var i=0; playlist_result.length > i; i++ ) {
        var checked = "";

        if(playlist_result[i].playlist_id != null) {
            checked = "checked";
        }
    %>
    <input type="checkbox" name="song_id" <%= checked %> value="<%= playlist_result[i].song_id %>"> <%= playlist_result[i].title + ' <> ' + playlist_result[i].artist + ' <> ' + playlist_result[i].album %>
    <br />
    <% } %>

    <br />
    <input type="submit" />


</form>
<br/><br/><a href="/playlist/all">Return to All Playlists</a>
<link rel='stylesheet' href='/stylesheets/style.css' />
</body>

</html>