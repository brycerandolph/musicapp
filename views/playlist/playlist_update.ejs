<!DOCTYPE html>
<html>
<head>
    <title>Playlist - Edit</title>
</head>
<h1>Edit Playlist</h1>
<body>

<form action="/playlist/update" method="get">

    <input type="hidden"
           id="playlist_id"
           name="playlist_id"
           value="<%= songs.playlist_id %>"
    />

    <span style="font-weight:bold">User</span> <br />

    <select name= 'user_name'>
        <% for(var i=0; users.length > i; i++ ) {
            var selected = "";
            if(users[i].user_name != null)
            {
                selected = "selected";

            }%>
        <option  <%= selected %> value="<%= users[i].user_name%>"> <%= users[i].user_name %>

            <% } %>
    </select>
    <br /><br />

    <span style="font-weight:bold">Playlist Name: </span></br><input
            id="name"
            name="name"
            type="text"
            value="<%= songs.playlist_name%>"
    />
    </br><br />

    <span style="font-weight:bold"> Songs </span> <br /> <br />
    <% for(var i=0; songs.length > i; i++ ) {
        var checked = "";

        if(songs[i].playlist_id != null) {
            checked = "checked";
        }
    %>
    <input type="checkbox" name="song_id" <%= checked %> value="<%= songs[i].song_id %>"> <%= songs[i].title + ' <> ' +songs[i].artist + ' <> ' + songs[i].album %>
    <br />
    <% } %>

    <br />
    <input type="submit" />


</form>
<br/><br/><a href="/playlist/all">Return to All Playlists</a>
<link rel='stylesheet' href='/stylesheets/style.css' />
</body>

</html>