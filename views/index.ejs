<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>A Project by Bryce Randolph</p>
<br/><br/>
    Please Select an Account <br />
    <form action="/user/hub" method="get">
        <select name= 'user_name'>
        <% for(var i=0; user_result.length > i; i++ ) {
          var selected = "";
          if(user_result[i].user_name != null)
          {
            selected = "selected";

          }%>
      <option  <%= selected %> value="<%= user_result[i].user_name %>"> <%= user_result[i].user_name %>

          <% } %>
    </select>

      <input type="submit" />
    </form>

    <!--<h1> Please create an account</h1>
    <form action="/user/insert" method="get">

      Username: <input name="user_name" type="text" />
      <br />
      Password: <input name="password" type="text" />
      <br />

      <input type="submit" />

    </form>-->
  </body>
</html>
